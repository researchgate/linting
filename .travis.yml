language: node_js
node_js:
  - "8"
  - "7"
  - "6"
cache: yarn
before_install: yarn global add greenkeeper-lockfile@1
before_script:
  - greenkeeper-lockfile-update
  - 'if [ -n "$TRAVIS_CI" ]; then yarn run create-github-release; fi'
script:
  - yarn lint
after_script: greenkeeper-lockfile-upload
